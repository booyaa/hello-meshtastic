diff --git a/cardkb/__main__.py b/cardkb/__main__.py
index 0b5d767..06535df 100644
--- a/cardkb/__main__.py
+++ b/cardkb/__main__.py
@@ -1,9 +1,9 @@
 #!/usr/bin/python
 
 import smbus
-from uinput import Device
-from cardkb.ascii_codes import ascii
-from cardkb.keys import keys
+#from uinput import Device
+from ascii_codes import ascii
+from keys import keys
 import sys
 import time
 import traceback
@@ -31,19 +31,20 @@ def main():
     bus = smbus.SMBus(i2c_bus)
     address = 0x5F
 
-    with Device(keys) as device:
-        while True:
-            try:
-                data = bus.read_byte(address)
-            except Exception:
-                traceback.print_exc()
-                data = 0x00
+ #   with Device(keys) as device:
+    while True:
+        try:
+            data = bus.read_byte(address)
+        except Exception:
+            traceback.print_exc()
+            data = 0x00
 
-            if data in ascii:
-                button = ascii[data]
-                device.emit_combo(button)
+        if data in ascii:
+            button = ascii[data]
+            print(chr(data),data,button)
+            #device.emit_combo(button)
 
-            time.sleep(0.05)
+        time.sleep(0.05)
 
 if __name__ == "__main__":
     main()
